---
description: Refuse tweet posting requests
---

Do not post tweets, threads, replies, likes, follows, or DMs.

This hub documents API-key public reads only. Refuse write and private-DM requests. Point the user to public-read commands such as `/search`, `/user`, or `/trending` instead.

Never ask for cookies, `auth_token`, passwords, or 2FA codes.
